# 🕸️ Mesh Web Client

Веб-клиент для decentralized P2P мессенджера Mesh с полной совместимостью с Android-клиентом.

## Особенности

✅ **Ed25519 криптография** — надежная идентификация  
✅ **WebRTC P2P** — прямые соединения между клиентами  
✅ **WebSocket fallback** — автоматический переход на сервер  
✅ **IndexedDB storage** — локальное хранение сообщений  
✅ **Mesh Score** — расчет сетевой репутации (L1 + 0.3×L2)  
✅ **Mnemonic backup** — восстановление через recovery phrase  
✅ **Cross-platform** — совместимость с Android клиентом  

## Установка

```bash
cd web-client
npm install
```

## Запуск

```bash
npm run dev
```

Приложение запустится на `http://localhost:3000`

## Сборка

```bash
npm run build
```

Статические файлы будут в папке `dist/`

## Архитектура

```
src/
├── core/
│   ├── crypto/           # Ed25519, AES-GCM encryption
│   ├── storage/          # IndexedDB (Dexie)
│   ├── network/          # WebSocket + WebRTC
│   └── mesh/             # Mesh Graph Manager
├── features/
│   ├── identity/         # Onboarding, identity management
│   └── invite/           # Invite creation/processing
└── ui/
    ├── HomeScreen        # Contacts list
    ├── ChatScreen        # Messaging interface
    └── ProfileScreen     # User profile & stats
```

## Использование

### Создание Identity

1. Откройте приложение
2. Нажмите "Create New Identity"
3. Введите nickname
4. Сохраните mnemonic фразу (для восстановления)

### Добавление контактов

1. Нажмите "Share Invite" для получения ссылки
2. Отправьте ссылку через любой мессенджер
3. Получатель откроет ссылку и автоматически добавится

### Отправка сообщений

1. Выберите контакт из списка
2. Введите сообщение  
3. Автоматически используется P2P (если доступно) или server fallback

## Протокол

### Шифрование

- **Identity**: Ed25519 keypair
- **Messages**: AES-256-GCM
- **Session keys**: HKDF-derived from sorted MeshIDs

### Mesh Score

```
MeshScore = L1 + 0.3 × L2
```

- **L1**: Прямые приглашенные
- **L2**: Приглашенные вашими приглашенными

### WebRTC + Server Fallback

1. Попытка установить P2P через WebRTC
2. Если неудачно → автоматический fallback на WebSocket server relay
3. Прозрачно для пользователя

## Совместимость с Android

Веб-клиент полностью совместим с Android клиентом:

- ✅ Одинаковый протокол шифрования
- ✅ Одинаковая структура сообщений
- ✅ Совместимый WebRTC signaling
- ✅ Общий сервер (`ws://34.78.2.164:8001/ws`)

## Development

```bash
# Install dependencies
npm install

# Run dev server
npm run dev

# Build for production
npm run build

# Preview production build
npm run preview
```

## Технологии

- **React 18** — UI framework
- **TypeScript** — Type safety
- **Vite** — Build tool
- **@noble/ed25519** — Ed25519 crypto
- **Dexie** — IndexedDB wrapper
- **WebRTC API** — P2P connections

## Безопасность

- 🔐 **End-to-end encryption** — все сообщения зашифрованы
- 🔑 **Local key storage** — приватные ключи только на устройстве
- ✍️ **Signature verification** — все invites подписаны
- 🛡️ **Anti-replay** — защита от повторных атак

## TODO

- [ ] Service Worker для offline support
- [ ] PWA manifest для установки на mobile
- [ ] L2 notifications
- [ ] Network map visualization
- [ ] File sharing

## License

MIT
