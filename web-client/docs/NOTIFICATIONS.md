# Система уведомлений в Mesh Web Client

## Обзор

Система уведомлений использует Web Notifications API и Service Worker для отправки браузерных push-уведомлений о важных событиях в приложении.

## Возможности

### 1. **Уведомления о новых сообщениях** 📧
- Отображается имя отправителя (nickname)
- Показывается текст сообщения (до 100 символов)
- Уведомления не показываются, когда приложение активно и в фокусе

### 2. **Уведомления о новых контактах** 👥
- Уведомление при добавлении нового контакта
- Отображается nickname нового контакта

### 3. **Уведомления о L2 соединениях** 🔗
- Информирование о новых L2 соединениях в mesh-сети
- Показывается через какой контакт произошло соединение

## Как включить уведомления

1. **Откройте профиль** - нажмите кнопку "Profile" в главном экране
2. **Перейдите в настройки уведомлений** - нажмите на кнопку "🔔 Настройки уведомлений"
3. **Разрешите уведомления** - нажмите "Включить уведомления" и разрешите в браузере

## Технические детали

### Компоненты системы

#### 1. **NotificationManager** (`src/utils/NotificationManager.ts`)
Singleton-класс для управления уведомлениями:
- Запрос разрешений
- Отображение уведомлений
- Умное определение когда показывать уведомления (не показывает при активном окне)
- Интеграция с Service Worker

#### 2. **Service Worker** (`public/sw.js`)
- Обработка событий `notificationclick`
- Открытие/фокусировка приложения при клике на уведомление
- Управление жизненным циклом уведомлений

#### 3. **NotificationSettings** (`src/ui/NotificationSettings.tsx`)
UI компонент для управления настройками:
- Показ статуса разрешений
- Включение/выключение уведомлений
- Тестирование уведомлений
- Информация о типах уведомлений

### Интеграция в HomeScreen

Уведомления автоматически отправляются при:
- Получении нового сообщения (`onMessageReceived`)
- Получении нового инвайта (`onInviteReceived`)
- Обнаружении L2 соединения (`l2-update` event)

## Поддержка браузеров

### Полная поддержка:
- ✅ Chrome/Edge 50+
- ✅ Firefox 44+
- ✅ Safari 16+ (macOS 13+)
- ✅ Opera 37+

### Ограничения:
- ❌ iOS Safari < 16.4 (частичная поддержка)
- ❌ Старые браузеры без Notification API

## Конфиденциальность и безопасность

- ✅ Уведомления работают локально через браузер
- ✅ Не требуется регистрация в push-сервисах
- ✅ Текст сообщений показывается только если разрешено пользователем
- ✅ Уведомления автоматически закрываются при клике

## Как отключить уведомления

### Способ 1: В приложении
1. Profile → Настройки уведомлений
2. Браузер не предоставляет API для отзыва разрешений программно
3. Нужно сделать в настройках браузера

### Способ 2: В браузере
**Chrome:**
1. Настройки → Конфиденциальность и безопасность → Настройки сайтов
2. Уведомления → Найти ваш сайт → Заблокировать

**Firefox:**
1. about:preferences#privacy
2. Разрешения → Уведомления → Настройки
3. Найти сайт и удалить/заблокировать

## Разработка и отладка

### Тестирование уведомлений
```typescript
import { notificationManager } from './utils/NotificationManager';

// Тестовое уведомление
await notificationManager.showNotification('Тест', {
    body: 'Тестовое сообщение',
    icon: '/logo.png'
});
```

### Проверка разрешений
```typescript
console.log('Permission:', Notification.permission);
console.log('Enabled:', notificationManager.isEnabled());
```

### Отладка Service Worker
1. Откройте DevTools → Application → Service Workers
2. Проверьте статус Service Worker
3. Смотрите логи в Console

## Известные проблемы

### Уведомления не показываются
1. **Проверьте разрешения**: `Notification.permission` должно быть `'granted'`
2. **Service Worker**: Убедитесь что SW зарегистрирован и активен
3. **HTTPS**: Уведомления требуют HTTPS (или localhost для разработки)
4. **Фокус окна**: Уведомления не показываются когда окно в фокусе (by design)

### Vibrate API
TypeScript может показывать ошибку для `vibrate`, но это валидная опция для мобильных браузеров. Используется `as any` для обхода проверки типов.

## Будущие улучшения

- [ ] Настройка типов уведомлений (включить/выключить по отдельности)
- [ ] Звуковые уведомления
- [ ] Группировка уведомлений от одного контакта
- [ ] Расширенные действия в уведомлениях (Quick Reply)
- [ ] Статистика уведомлений
- [ ] Quiet hours (не беспокоить в определенное время)
