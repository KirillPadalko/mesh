# 🕸️ Mesh — Спецификация инвайтов и Mesh-Score (MVP)

## Назначение документа

Описать:

* как начисляется Mesh-Score через приглашения
* как работает P2P-протокол инвайтов (L1 + L2)
* как отображается Mesh-Score в UI **без чисел**
* какие ограничения и допущения приняты в MVP

---

## 1. Основные определения

* **Mesh-ID** — публичный ключ пользователя
* **Узел** — пользователь в сети Mesh
* **L1** — пользователь, приглашённый напрямую
* **L2** — пользователь, приглашённый твоим приглашённым
* **Mesh-Score** — мера включённости узла в сеть (не рейтинг)

---

## 2. Принципы (обязательные)

1. Нет аккаунтов, логинов, телефонов
2. Инвайты и score считаются **без сервера**
3. Учитываются **только подтверждённые связи**
4. Глубина приглашений ограничена **L2**
5. Mesh-Score — **приближённый**, а не консенсусный
6. Накрутка должна быть экономически бессмысленной

---

## 3. Формула Mesh-Score (жёстко фиксирована)

Учитывается **только реферальный вклад**:

```
MeshScore = L1 + 0.3 × L2
```

Где:

* `L1` — количество подтверждённых прямых приглашений
* `L2` — количество подтверждённых приглашений второго уровня
* `L3+` — игнорируются

---

## 4. P2P-протокол инвайтов

### 4.1 INVITE (A → B)

```json
{
  "type": "invite",
  "from": "mesh_id_A",
  "to": "mesh_id_B",
  "timestamp": 1710000000,
  "signature": "sig_A(from + to + timestamp)"
}
```

Правила:

* B принимает **только первый invite**
* invite сохраняется локально
* без подтверждения очки не начисляются

---

### 4.2 INVITE_ACK (B → A)

```json
{
  "type": "invite_ack",
  "from": "mesh_id_B",
  "to": "mesh_id_A",
  "invite_hash": "hash(invite)",
  "timestamp": 1710000100,
  "signature": "sig_B(...)"
}
```

После ACK:

* связь A ↔ B считается подтверждённой
* A получает **+1 L1**

---

## 5. L2-механика (передача цепочки)

### 5.1 INVITE с цепочкой (B → C)

```json
{
  "type": "invite",
  "from": "mesh_id_B",
  "to": "mesh_id_C",
  "timestamp": 1710000200,
  "parent_invite": {
    "from": "mesh_id_A",
    "to": "mesh_id_B",
    "signature": "sig_A"
  },
  "signature": "sig_B(...)"
}
```

C узнаёт:

* кто его пригласил (B)
* что B был приглашён A
* глубина = 2

---

### 5.2 L2_NOTIFY (B → A)

Отправляется **после подтверждения C → B**.

```json
{
  "type": "l2_notify",
  "origin": "mesh_id_C",
  "via": "mesh_id_B",
  "proof": {
    "invite_AB": "...",
    "invite_BC": "..."
  },
  "timestamp": 1710000300,
  "signature": "sig_B(...)"
}
```

A проверяет:

* подпись B
* валидность цепочки A → B → C
* глубина ≤ 2

После проверки:

* A получает **+0.3 L2**

---

## 6. Защита от накрутки (протокольно)

* самоприглашения → невозможны (подписи)
* циклы → запрещены (первый invite фиксируется)
* глубина > 2 → игнорируется
* без ACK → 0 очков
* неактивные узлы не дают L2

---

## 7. Где хранится Mesh-Score

* **Источник истины**: локальный расчёт на узле
* **Сервер**: может строить граф (опционально), но не диктует score
* **Клиент**: хранит локальный кеш

Mesh-Score **привязан к Mesh-ID**, не к устройству.

---

## 8. UI-интерпретация Mesh-Score (без чисел)

Mesh-Score **не показывается числом**.

Используется абстрактный статус узла — **Mesh Signal**.

### Уровни (пример)

Уровни
Score	Форма - это маленькая иконка вместо значка профиля - пульсирующая слабое неоновое свечение
0	точка
+1	две точки +2 треугольник
+3	квадрат
+4	пятиугольник
+5	опять точка но другого цвета и с более ярким свечением

### Где показывается

* Profile:
  `Статус узла: Connected`
* Mesh Map:

  * размер точки = статус
  * толщина связей = активность
* Ограничения функций:

  > Функция станет доступна, когда сеть узнает тебя лучше

---

## 9. Что НЕ делаем в MVP

* L3 и глубже
* глобальный консенсус score
* цифры и рейтинги
* серверную верификацию инвайтов
* финансовые стимулы

---

## 10. Ожидаемый результат

* Mesh-Score растёт только через живые связи
* Накрутка не даёт преимущества
* Пользователь **чувствует рост**, но не “фармит”
* Система работает без центра
* Архитектура готова к расширению

---

## Ключевая мысль (для команды)

> Mesh-Score — это не счёт.
> Это след твоего присутствия в сети.
